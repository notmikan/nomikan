# 🤖 仕様書: SNS ＆ タイムテーブル Gemini AI 連動システム (確定版)

## 1. 概要と目的
サークル統合Webアプリにおいて、部員がSNS（タイムライン）で投稿した際、**Gemini 1.5 Flash AI** がバックグラウンドで投稿内容を解析し、タイムテーブル上の該当タスクの進捗率（%）および作業 ToDo を自動更新・提案するシステムです。

---

## 2. 🛡️ 誤作動防止 ＆ 超エコ・トークン節約設計

### 2.1 班タグ必須トリガー (雑談時のトークン消費0)
- 投稿時に選択されたハッシュタグが **`#雑談` または未指定の場合は、AIを一切起動しない（トークン消費完全0）**。
- `#FRP` `#翼` `#コクピ` `#電装` `#運営` の各班タグが指定された投稿のみ AI 解析を実行。

### 2.2 班内限定マッピング (誤検索ゼロ)
- 投稿された班に紐づく**未完了タスク（数件）のみ**を Gemini API に送信。
- FRPの投稿で他班（翼やコクピなど）のタスクが誤選出されるリスクを物理的にゼロ化。

---

## 3. 🧠 確信度 (Confidence) に基づく2段階アクション

### 3.1 確信度 高 (Confidence ≥ 80%) ➔ 【自動更新 ＆ ニュアンス打ち分け】
- Gemini が「間違いなくこのタスクの作業だ」と確信した場合、投稿文の文脈（ニュアンス）を判定：
  1. **【完了報告】(例:「〜おわった」「〜完了」)**:
     - 既存 ToDo に `[✓]` チェックを入れる。
     - バッジ: **`🤖 進捗更新 by Gemini (タスク名: [✓] 作業名)`** (緑色)
  2. **【進行中・課題報告】(例:「〜むずすぎ」「〜苦戦中」「〜動かす」)**:
     - ToDo にチェックは入れず、新しい未完了 ToDo `[ ]` を枠として自動追加する！
     - バッジ: **`🤖 ToDo枠追加 by Gemini (タスク名: [ ] 作業名)`** (青色)

### 3.2 確信度 中 (50% ≤ Confidence < 80%) ➔ 【提案・手動確認ボタン】
- Gemini が「関連しそうだが確証がない」と判定した場合：
  - タスクや ToDo を勝手に変更せず、投稿カード下部に確認ボタンを提示：
    `🤖「主桁積層」の進捗を更新しますか？ [ ＋更新する ]`

### 3.3 確信度 低 (Confidence < 50%) ➔ 【無反応】
- 作業に関連しない一般的な日常投稿と判定し、何もしない（0アクション）。

---

## 4. 📊 サマリー用 AI 操作ログ (AI Activity Logs)
- AIが自動更新または手動承認によってタスクを変更した履歴を Firebase (`hope_webapp_ai_logs`) に蓄積。
- サマリー画面で「🤖 AIによる直近の自動更新アクティビティ」として可視化可能。

---

## 5. ⚠️ AI使用時の注意点・ハマりどころ (Gotchas)

### 5.1 APIキーの形式と認証ヘッダー
- **`AQ.` から始まる新しい API キー**を使用する場合、URLパラメータ（`?key=AQ...`）で渡すと Google サーバーで **404 (Not Found)** になります。
- 必ずリクエストヘッダー **`'X-goog-api-key': apiKey`** を指定して送信する必要があります。

### 5.2 モデル名指定の表記
- エンドポイントのモデル名は `gemini-1.5-flash` または `gemini-flash-latest` を使用してください。

### 5.3 画面再レンダリングの必須性
- 非同期（`async/await`）で AI 解析を行った後、データを保存 (`savePosts`) するだけでなく、必ず **`renderPostsTimeline()` (タイムライン再描画関数)** を明示的に呼び出さないと、画面のバッジがリアルタイム更新されません。

---

## 6. 🛠️ 実装時の改善方法 ＆ 今後のメンテナンス方針

### 6.1 ウルトラ・ハイブリッド判定エンジン (セーフティネット)
- 万が一 Google API のレスポンスがネットワーク障害や安全フィルターで崩れた場合でも、アプリを停止させないために **「ローカル・キーワードマッピング (fallbackLocalMatching)」** をバックアップとして併用しています。
- これにより、通信エラー時でも「基板」「サンディング」「リブ」などの単語からタスクを100%自動補完・判定し、アプリの可用性を担保します。

### 6.2 トークン消費量の極小化テクニック
- システムプロンプトを長文にせず、`ID:1, Task:桁焼き` のように1行に圧縮して送信することで、毎回の入力トークンを最小限に抑えています。
- 今後タスク数が増加した場合は、過去に完了したタスクをプロンプトから除外して送信することでトークン節約を維持してください。

---
*最終更新日: 2026年8月2日 (注意点・改善方法追記版)*
