# 📜 HoPE サークル統合Webアプリ 現状 ＆ 写真投稿機能 仕様書

## 1. 概要・コンセプト
鳥人間コンテストに向けた部室・作業場での日常進捗、作業ログ、雑談、現場写真、連絡事項をサークル全員でリアルタイムに一元共有・管理するための **サークル統合型Webアプリケーション**。

---

## 2. 📷 写真投稿 ＆ クライアントWebP自動圧縮仕様 (NEW / 追加仕様案)

### 2.1 写真添付の概要
* 部室での作業成果（主桁・翼組み・電装基板など）の現場写真を、タイムラインの投稿に添付して共有可能。
* スマホのカメラ撮影、またはアルバム/ファイル選択に対応。

### 2.2 超軽量 クライアント側 WebP 自動圧縮・リサイズロジック
スマホで撮影した数MB〜十数MBの大容量写真をそのまま送信すると、通信量の消費やデータベースの圧迫を引き起こすため、**送信前にブラウザ（Canvas API）で瞬時に超軽量化** を行う。

* **自動圧縮・リサイズパラメータ**:
  * **最大解像度**: 長辺 **1000px**（縦横比維持で自動リサイズ）
  * **出力フォーマット**: **`image/webp`** (画質 Quality: 0.75 / 75%)
  * **データサイズ**: 元画像が 10MB であっても **30KB 〜 80KB** 前後（約1/100以下）まで自動軽量化。
  * **処理場所**: 端末ローカルブラウザ上（サーバーやDBへ負担をかけず0.1秒で変換完了）。

### 2.3 タイムライン上の画像UI表現
* **投稿入力画面**: 「📷 写真」ボタンから画像選択。プレビューサムネイルと取り消し（✕）ボタン。
* **タイムラインカード**: X (Twitter) と同様の上品な角丸画像プレビュー枠。タップすると拡大鑑賞モーダルが開く。

### 2.4 データ構造 (JSON)
Firebase Realtime Database (`hope_webapp_posts` ノード) に軽量化データURI（Base64）としてダイレクト保存。

```json
{
  "id": "post_1785567890123",
  "author": "山田",
  "team": "FRP", 
  "userId": "usr_1785561234567_a8b9",
  "content": "主桁の積層完了！明日の午前中に脱型します。",
  "images": [
    "data:image/webp;base64,UklGRvAAAABXRUJQVlA4..."
  ],
  "createdAt": 1785567890123,
  "likedBy": ["usr_1785561234567_a8b9"],
  "replies": []
}
```

---

## 3. モジュール別画面構造 ＆ ナビゲーション

### 3.1 ヘッダー・レスポンシブナビゲーション
* **PC・デスクトップ時**:
  * ヘッダー内タイトル: シンプルな **`HoPE`** テキストロゴ。
  * ヘッダーナビ: `ホーム` `タイムテーブル` `サマリー (新設計)` `在庫 (Soon)` のスタイリッシュなタブ。
* **📱 スマホ表示時 (< 900px)**:
  * 上部ヘッダーのタブは非表示となり、画面最下部に**透過固定ボトムナビゲーションバー**（`🏠 ホーム` `🗓️ タイムテーブル` `📊 サマリー` `📦 在庫`）が常時固定表示。
  * ※「予算管理」は信頼性・データ保全の観点からGoogleスプレッドシートへ移行・独立分離し、本アプリ内は「全体向け＆各班用 サマリーダッシュボード」へブラッシュアップ。

---

## 4. サークルSNS仕様（タイムライン ＆ 投稿モーダル ＆ ハッシュタグ）

### 4.1 タイムライン・フィード構造
1. **一元集約メインフィード**:
   * 全投稿（作業ログ・雑談・連絡・現場写真）が時系列で流れる統合フィード。
2. **洗練されたハッシュタグフィルター**:
   * 上部スライドタブ: `すべて` `#雑談` `#運営` `#FRP` `#翼` `#コクピ` `#電装` で特定タグの投稿のみを抽出閲覧。

### 4.2 投稿インターフェース ＆ 🤖 AI自動タスク判定
* **右下固定 浮遊投稿ボタン (FAB)**:
  * ホーム画面が開いている時のみ、画面右下に固定浮遊ボタン（Twitter風 Compose Icon）を表示。
  * ボタンをタップすると、画面中央にスムーズな **投稿ダイアログモーダル** が出現し、テキスト＋WebP圧縮写真の投稿を完了。
* **🤖 AIによる自動タスク判別連携**:
  * 投稿時、AIが本文・写真を解析し、タイムテーブル上の該当タスクへ自動紐付け。進捗ログとして記録。

---

## 5. ユーザー管理仕様

* **超軽量プロファイル記憶**:
  * ユーザーの所属班は固定せず、「表示名（お名前）」のみをブラウザ `LocalStorage` に自動記憶。
  * 初回投稿時または設定ボタンからいつでも1秒で変更可能。

---
*最終更新日: 2026年8月1日 (写真投稿・WebP自動圧縮仕様 追加版)*
